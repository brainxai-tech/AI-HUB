{
  "name": "ai-project-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "node --check server.mjs && node --check public/app.js && node --check public/key-config/key-config.js && node --check public/model-guide-data.js && node --check public/model-guide.js && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
